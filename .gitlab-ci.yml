build:api:
  image: microsoft/dotnet:latest
  script:
    - dotnet --info
    - dotnet restore
    - dotnet build

build:angular:
  image: node:latest
  script:
    - pwd
    - cd web
    - yarn install
    - yarn run ng build
